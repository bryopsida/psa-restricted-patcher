import { describe, it, expect } from '@jest/globals'
import 'reflect-metadata'

describe('services/kubernetes', () => {
  describe('syncCaBundle', () => {
    it('should update web hook configuration when caBundle does not match', () => {
      expect(false).toBe(true)
    })
    it('should not update web hook configuration when caBundle matches', () => {
      expect(false).toBe(true)
    })
    it('should resolve false on error', () => {
      expect(false).toBe(true)
    })
  })
})
